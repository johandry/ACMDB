<%= simple_form_for(@team) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :email %>
    <%= f.association :system %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
